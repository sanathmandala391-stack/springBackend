services:
  - type: web
    name: serveease-backend
    runtime: java
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/saibaba-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
